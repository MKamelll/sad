import "std.stdio";

fn fac(n: int, result: int): int {
    if n < 2 {
        return result;
    }

    return fac(n - 1, n * result);
}

fn main(): int {
    let i: int = 15;
    let x: int = 5;
    let s: string = "Hello";
    writeln(s);
    return 0;
}